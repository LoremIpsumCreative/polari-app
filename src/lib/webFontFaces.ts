// Native counterpart to webFontFaces.web.ts. On iOS/Android the fonts come from
// expo-font (see fontAssets.ts), so there is no CSS to inject.
export function installWebFonts() {}

export const webFontsReady = true;
